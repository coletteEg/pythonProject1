print("I can't believe we are starting up Specs")
len() #prints out length of whatever is entered
/n #makes a new line
/t #Makes a tab
variable_name[number] #you can use this to have it pull whatever is at the place you enter
[2:]# index to all the way to the end
[:3]#index up to the place but dont include it
[3:8] #start but dont include 3 and end but dont include 8
[::-1]#reverse a string
pip replaces npm
python -m venv (name of virtual env)
add virtual environment to .gitignore
source (virtual environment name)/scripts/activate
pip freeze looks at the things you have installed onto your environment 
pip freeze > requirements.txt #moves all the things installed and lets people know that people need to install them
